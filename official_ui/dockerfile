# Use Node 20 (Stable LTS)
FROM node:24-alpine

WORKDIR /app
RUN apk add --no-cache git
COPY package*.json ./
RUN npm install
COPY . .

# ✅ CHANGED: Expose 3000
EXPOSE 3000

# ✅ CHANGED: Force Vite to use port 3000
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]